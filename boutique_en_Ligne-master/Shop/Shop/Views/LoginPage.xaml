<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:Shop.ViewModels" x:DataType="viewmodels:LoginViewModel"
             x:Class="Shop.Views.LoginPage"
             Title="Login">

    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Padding="20" Margin="0,0,0,500">
            <!-- Réduisez la marge en haut -->
            <Entry Placeholder="Username" Text="{Binding Username}" Margin="0,0,0,5" />
            <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="True" Margin="0,0,0,5" />
            <Button Text="Login" Command="{Binding LoginCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
